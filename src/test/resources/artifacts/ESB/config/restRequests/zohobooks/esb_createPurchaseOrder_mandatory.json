{
  "apiUrl": "%s(apiUrl)",
  "authToken": "%s(authToken)",
  "organizationId": "%s(organizationId)",
  "vendorId": "%s(vendorId)",
  "lineItems": [
    {
      "item_id": "%s(itemIdOptional)"
    }
  ]
}
